name: iOS unsigned build

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main

permissions:
  contents: read

concurrency:
  group: ios-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build unsigned iPhone app
    runs-on: macos-latest
    timeout-minutes: 90

    steps:
      - name: Check out source and llama.cpp
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Record toolchain
        run: |
          set -euxo pipefail
          xcodebuild -version
          swift --version
          cmake --version

      - name: Build llama.cpp XCFramework
        run: |
          set -o pipefail
          make framework 2>&1 | tee llama-framework-build.log

      - name: Resolve Swift package dependencies
        run: |
          set -o pipefail
          xcodebuild \
            -resolvePackageDependencies \
            -project openTihui.xcodeproj \
            -scheme openTihui \
            -clonedSourcePackagesDirPath build-ci/SourcePackages \
            -skipPackagePluginValidation \
            2>&1 | tee package-resolve.log

      - name: Apply Qwen3-TTS 0.2.0 ICL loader compatibility patch
        run: |
          set -euxo pipefail
          PACKAGE_DIR='build-ci/SourcePackages/checkouts/mlx-swift-qwen3-tts'
          test -d "$PACKAGE_DIR"
          git -C "$PACKAGE_DIR" apply --check "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-icl-loader.patch"
          git -C "$PACKAGE_DIR" apply "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-icl-loader.patch"
          git -C "$PACKAGE_DIR" apply --check "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-icl-generation.patch"
          git -C "$PACKAGE_DIR" apply "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-icl-generation.patch"
          git -C "$PACKAGE_DIR" apply --check "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-full-icl-codec.patch"
          git -C "$PACKAGE_DIR" apply "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-full-icl-codec.patch"
          git -C "$PACKAGE_DIR" apply --check "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-byte-tokenizer.patch"
          git -C "$PACKAGE_DIR" apply "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-byte-tokenizer.patch"
          git -C "$PACKAGE_DIR" apply --check "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-prefill-memory.patch"
          git -C "$PACKAGE_DIR" apply "$GITHUB_WORKSPACE/patches/qwen3tts-0.2.0-prefill-memory.patch"

      - name: Build unsigned device app
        run: |
          set -o pipefail
          xcodebuild \
            -project openTihui.xcodeproj \
            -scheme openTihui \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build-ci \
            -clonedSourcePackagesDirPath build-ci/SourcePackages \
            -skipPackagePluginValidation \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY='' \
            build 2>&1 | tee xcodebuild.log

      - name: Package unsigned IPA
        run: |
          set -euxo pipefail
          APP_PATH='build-ci/Build/Products/Release-iphoneos/openTihui.app'
          test -d "$APP_PATH"
          STAGE_DIR="$(mktemp -d)"
          mkdir -p "$STAGE_DIR/Payload"
          ditto "$APP_PATH" "$STAGE_DIR/Payload/openTihui.app"
          (
            cd "$STAGE_DIR"
            /usr/bin/zip -qry "$GITHUB_WORKSPACE/openTihui-unsigned.ipa" Payload
          )
          ditto -c -k --sequesterRsrc --keepParent "$APP_PATH" openTihui-unsigned-app.zip

      - name: Upload unsigned build
        uses: actions/upload-artifact@v4
        with:
          name: openTihui-unsigned-${{ github.run_number }}
          path: |
            openTihui-unsigned.ipa
            openTihui-unsigned-app.zip
          if-no-files-found: error
          retention-days: 14

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openTihui-build-logs-${{ github.run_number }}
          path: |
            llama-framework-build.log
            package-resolve.log
            xcodebuild.log
          if-no-files-found: warn
          retention-days: 14
